def obtenerHorasTrabajadas():
    horasTotales = []
    for i in range(len(listaHorasInicio)):
        horasMinutosInicio=listaHorasInicio[i].split(':')
        horasMinutosFin=listaHorasFin[i].split(':')
        horaInicio= int(horasMinutosInicio[0])
        horaFin= int(horasMinutosFin[0])
        minutosInicio= int(horasMinutosInicio[1])
        minutosFin= int(horasMinutosFin[1])
        if horaFin >= horaInicio and minutosFin >= minutosInicio:
            print("Horas Totales: " + str(horaFin-horaInicio) +':'+ str(minutosFin-minutosInicio))
        elif horaFin >= horaInicio and minutosFin < minutosInicio:
            print("Horas Totales: " + str(horaFin-horaInicio-1) + ':'+ str(minutosFin+minutosInicio))
